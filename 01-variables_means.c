// Variable
// A variable is a container that stores a ‘value.’ variable in c stores the value of a constant.

// a = 3 (a is assigned “3”)
// b = 4.7 (b is assigned “4.7”)
// c = 'A' (c is assigned “A”)

#include <stdio.h>

int main()
{
 int dollar;
 int _dollar;
 int _error_code;
 int Dollar;
 return 0;
}

// Rules for naming variables in c:

// The first character must be an alphabet or underscore(_)
// No commas or blanks are allowed
// No special symbol other than underscore is allowed
// Variable names are case sensitive